#include "stdafx.h"
#include "UI.h"

UI::UI()
{
}

UI::~UI()
{
}

void UI::Init()
{/*
	m_Time = Sprite::Create(L"Painting/UI/infinite.bmp");
	m_Time->SetScale(3, 3);
	m_Time->m_Tag = "UI";

	m_Time->SetPosition(800 / 2 - (m_Time->m_Size.x * m_Time->m_Scale.x) / 2, 30);

	m_PlayerFrame = Sprite::Create(L"Painting/UI/player_frame.bmp");
	m_PlayerFrame->SetScale(3, 3);
	m_PlayerFrame->m_Tag = "UI";

	m_PlayerFrame->SetPosition(m_Time->m_Position.x - 200, 30);

	m_HpBar = Sprite::Create(L"Painting/UI/hpbar.bmp");
	m_HpBar->SetScale(3, 3);
	m_HpBar->m_Tag = "UI";

	m_HpBar->SetPosition(m_PlayerFrame->m_Position.x - 100, 50);

	m_1Up = Sprite::Create(L"Painting/UI/1up.bmp");
	m_1Up->SetScale(3, 3);
	m_1Up->m_Tag = "UI";*/
}

void UI::Release()
{
}

void UI::Update()
{
}

void UI::Render()
{/*
	m_Time->Render();
	m_PlayerFrame->Render();
	m_HpBar->Render();
	m_1Up->Render();*/
}
