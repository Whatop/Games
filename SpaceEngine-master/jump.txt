
	점프를 하면
	if('W')
	{
		Pos = m_Position; //점프전 위치 기억
		m_Move=state::Jump;
	}
	if(m_Move==state::Jump)
	{
		if(Pos>
	}
	
	

	중력가속도때문에 빨리떨어진다
	void Heart::Gravity()
	{
		static float vy = 0;
		vy += 9.8f * dt;

		if (!m_isGround) //땅 안닿았을때
		{
			m_Position.y += vy;
		}
		else // 닿으면 중력가속도 초기화
			vy = 0.f;
		if (m_Gravity == _left) {
			
		}
		else if (m_Gravity == _right) {

		}
		else if (m_Gravity == _up){

		}
		else if (m_Gravity == _down) {

		}
	
	}
